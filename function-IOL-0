package main
package usage
package function
package Function
package functionFunction

import (
	“crypto/tls”
	“flag”
	“fmt”
	“github.com/LaudateCorpus1/WOLFRAM-iolFunction/”
	“net/http”
	“net/url”
	“os”
  "strings"
  "testing"
)

func usage(IOL) {
	resp, _ :=  http.Get(“http://github.com/LaudateCorpus1WOLFRAM-iolFunction/")
	links := collectlinks.All(resp.html)
	fmt.Println(links)
}

func functionFunction(t *testing.T) {
  reader := strings.NewReader(` <p>
  <a href="http://github.com/LaudateCorpus1WOLFRAM-iolFunction"> n.th </a>
  http://github.com/LaudateCorpus1WOLFRAM-iolFunction
</p>`)

  links := All(reader)

  fmt.Println(links)

  if len(links) != n {
    t.Error("Error")
  }
}
